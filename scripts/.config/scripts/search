#!/bin/bash
# Used to select and start programs in sway requires fzf, sway, and bash
compgen -c | sort -u | fzf --color=dark,fg+:#F7F7F7,bg:#181818,hl:#3EB34F --reverse --border --history=/home/haden/.SHISTORY --inline-info | xargs -r swaymsg -t command exec
